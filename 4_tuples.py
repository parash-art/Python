#create the tuples in ()
t = (1, 2, 3, 4, 5, 9)
#t1 = () empty tuples
# t1 = () # wrong way to declare a tuple with single element
t1 = (1,) # single element tuples
print(t1)
 # A tuples is an immutable data type in python.
#printing the elements of a tuples
#print(t[0])
#cannot update the values of a tuple
#t[0] = 35 throws an error